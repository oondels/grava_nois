# Relatorio Diario — 2026-03-05

## [Pre-dev] AdminVenues com cards mobile e edicao de venue/device
PRE-DEV REPORT: Implementar edicao de venues no admin com botao de acao e layout mobile em cards

GOAL: na pagina `AdminVenues.vue`, adicionar botao de editar por item e substituir tabela por cards em mobile, integrando com `PATCH /admin/venues/:id` para atualizar dados da venue e credenciais de device.

DEFINITION OF DONE:
- Desktop com coluna de acoes e botao `Editar`.
- Mobile sem tabela, exibindo cards por venue.
- Dialog de edicao com campos de venue + credenciais de device.
- Integracao no service frontend com `updateVenue`.
- Lista atualizada apos salvar com sucesso.
- Build do frontend passando.

CODEBASE READING COMPLETE:
- `specs/grava-nois/DESIGN_SPEC.md`
- `grava_nois/README.md`
- `grava_nois/src/pages/admin/AdminVenues.vue`
- `grava_nois/src/pages/admin/AdminClients.vue`
- `grava_nois/src/pages/admin/AdminUsers.vue`
- `grava_nois/src/services/admin.service.ts`
- `grava_nois/src/router/index.ts`
- `.codex/skills/pre-dev/SKILL.md`
- `.codex/references/agents/pre-dev.md`

REUSE ASSESSMENT:
- Decisao: adaptar padroes existentes de `AdminClients` (cards mobile) e `AdminUsers` (dialogo de edicao).
- Reuso: componentes Vuetify ja padronizados no painel admin (`v-data-table`, `v-card`, `v-dialog`, `v-alert`).
- Justificativa: consistencia visual e menor risco de regressao.

HARD RULES CHECK:
- HR-001: aderente (consumo de rota admin autenticada/protegida).
- HR-002: sem impacto direto.
- HR-003..HR-010: sem impacto direto no escopo frontend.

IMPACT MAP:
- Arquivos diretos:
  - `grava_nois/src/pages/admin/AdminVenues.vue`
  - `grava_nois/src/services/admin.service.ts`
- Risco de cascata:
  - baixo/medio (mudanca de UX local na tela de quadras admin).
- Limite de escopo seguro:
  - nao alterar rotas globais nem outras paginas admin.

TEST PLAN:
- Validacao manual desktop e mobile da listagem e edicao.
- Validar salvamento com edicao manual de credenciais e com regeneracao.
- Validar exibicao de erros da API no dialog.
- Comandos:
  - `cd grava_nois && npm run build`
  - `cd grava_nois && npm run test` (se aplicavel; falha preexistente conhecida no repo)

READY TO IMPLEMENT: YES

## [Execucao] AdminVenues com botao editar e cards no mobile
- `src/services/admin.service.ts`
  - ampliado tipo `AdminVenue` com campos usados na edicao.
  - criado `UpdateVenuePayload` e `UpdateVenueResponse`.
  - adicionada funcao `updateVenue(id, payload)` consumindo `PATCH /admin/venues/:id`.

- `src/pages/admin/AdminVenues.vue`
  - desktop: adicionada coluna `Ações` com botao `Editar` por linha.
  - mobile: removida tabela e implementado layout em cards por venue.
  - criado dialog de edicao para atualizar dados de venue e credenciais de device.
  - adicionado suporte a regeneracao de credenciais (`regenerateDeviceCredentials`) e validacao minima de `deviceSecret`.
  - feedback de sucesso/erro apos salvamento e recarga da listagem.

## [Validacao]
- `cd grava_nois && npm run build` -> sucesso.
- `cd grava_nois && npm run test` -> falha preexistente do script Vitest (`Unknown option --setupFiles`).
