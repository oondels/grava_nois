# Relatorio Diario — 2026-03-04

## [Pre-dev] Hardening do token de sessao (grava_nois_api) + validacao de impacto no frontend
PRE-DEV REPORT: Corrigir exposicao desnecessaria de informacao no token de sessao e evitar uso de claims stale para autorizacao

GOAL: reduzir exposicao de dados no access token e remover dependencia de claims stale (`role`/`clientId`) na autorizacao do backend, mantendo compatibilidade funcional com o frontend.

DEFINITION OF DONE:
- Access token deixa de carregar `email`.
- Middleware de autenticacao nao depende de `role`/`clientId` do JWT para decidir autorizacao.
- `req.user` continua consistente para os middlewares/rotas atuais (`id`, `role`, `clientId`) com valores autoritativos do banco.
- Fluxo frontend permanece funcional sem depender de decode do JWT/cookie para email/sessao.
- Verificacoes de teste/build executadas para validar ausencia de regressao basica.

CODEBASE READING COMPLETE:
- `specs/grava-nois/DESIGN_SPEC.md`
- `grava_nois_api/README.md`
- `grava_nois_api/src/controllers/auth.controller.ts`
- `grava_nois_api/src/middlewares/auth.middleware.ts`
- `grava_nois_api/src/middlewares/admin.middleware.ts`
- `grava_nois_api/src/middlewares/client.middleware.ts`
- `grava_nois_api/src/routes/auth.route.ts`
- `grava_nois_api/src/routes/client.route.ts`
- `grava_nois_api/src/services/auth.service.ts`
- `grava_nois_api/src/models/User.ts`
- `grava_nois_api/src/models/Clients.ts`
- `grava_nois_api/src/types/express.d.ts`
- `grava_nois/src/store/auth.ts`
- `grava_nois/src/services/api.ts`
- `grava_nois/src/router/index.ts`
- `.codex/skills/pre-dev/SKILL.md`
- `.codex/references/agents/pre-dev.md`

REUSE ASSESSMENT:
- Decisao: adaptar fluxo existente (sem criar novo middleware/servico).
- Reuso:
  - Manter emissao de JWT em `auth.controller.ts`, apenas minimizando payload.
  - Manter `authenticateToken`, alterando fonte de verdade de `role/clientId` para banco.
- Justificativa: menor risco de regressao e menor superficie de mudanca com ganho direto de seguranca.

HARD RULES CHECK:
- HR-001 (rotas protegidas + admin): reforcado, pois `role` passa a vir da base autoritativa por request.
- HR-002 (isolamento por `clientId`): reforcado, pois `clientId` passa a ser resolvido do banco e nao de claim stale.
- HR-003..HR-010: sem impacto direto neste escopo.
- Status: aderente.

IMPACT MAP:
- Arquivos diretos:
  - `grava_nois_api/src/controllers/auth.controller.ts`
  - `grava_nois_api/src/middlewares/auth.middleware.ts`
- Arquivos chamadores afetados indiretamente:
  - `grava_nois_api/src/middlewares/admin.middleware.ts`
  - `grava_nois_api/src/middlewares/client.middleware.ts`
  - Rotas que dependem de `authenticateToken` (`auth`, `client`, `admin`, `video`, `payment`, `user`)
- Frontend (validacao de contrato):
  - `grava_nois/src/store/auth.ts`
  - `grava_nois/src/services/api.ts`
  - `grava_nois/src/router/index.ts`
- Risco de cascata:
  - medio no backend (auth em cadeia em varias rotas), baixo no frontend (nao decodifica JWT).
- Limite de escopo seguro:
  - nao alterar endpoints/contrato de resposta de `/auth/me` e login.

TEST PLAN:
- Backend:
  - validar login/sign-up/google/refresh continuam emitindo cookie de access token valido.
  - validar middlewares `requireAdmin` e `requireClient` continuam baseados em `req.user` consistente.
  - validar token antigo sem `role/clientId/email` ainda autentica desde que contenha `userId`.
- Frontend:
  - validar boot (`ensureReady` -> `/auth/me`) continua montando sessao.
  - validar guards de rota admin/client continuam funcionais.
- Comandos:
  - `cd grava_nois_api && npm run build`
  - `cd grava_nois && npm run build`

READY TO IMPLEMENT: YES

## [Execucao] Hardening do token e da autorizacao
- `grava_nois_api/src/controllers/auth.controller.ts`
  - removidos `email`, `role` e `clientId` do payload do JWT.
  - access token agora contem apenas `userId` + claims padrao (`iat`/`exp`).
- `grava_nois_api/src/middlewares/auth.middleware.ts`
  - autenticacao passou a exigir apenas `userId` (ou `id` legado) no token.
  - `role` e `clientId` agora sao resolvidos pelo banco por request.
  - `email` em `req.user` passa a vir do banco (nao do JWT).

## [Validacao] Frontend e build
- Frontend (`grava_nois`) revisado:
  - nao ha decode de JWT/cookie em `src` (nenhum uso de `jwt`, `atob`, `document.cookie` para sessao).
  - sessao e usuario continuam vindo de `/auth/me` e respostas de login (`src/store/auth.ts`).
  - conclusao: nenhum ajuste de frontend necessario para esta mudanca.
- Comandos executados:
  - `cd grava_nois_api && npm run build` -> sucesso (`tsc`).
  - `cd grava_nois && npm run build` -> sucesso (`vue-tsc -b && vite build`).
