# Relatorio Diario — 2026-03-02

## [Pre-dev] Correcao CORS no ViaCEP (frontend)
PRE-DEV REPORT: Corrigir falha de CORS/502 na consulta de CEP via `fetchViaCepAddress`

GOAL: eliminar falha de autopreenchimento de endereco quando o browser bloqueia resposta do ViaCEP por CORS, mantendo validacao de CEP e UX atual nas telas que usam `fetchViaCepAddress`.

DEFINITION OF DONE:
- `fetchViaCepAddress` deixa de depender de um unico provedor e passa a retornar endereco valido quando houver fallback disponivel.
- Fluxo atual de `ContactPage.vue` e `ClientProfile.vue` continua compatível sem alterar contrato do utilitario.
- Cobertura de teste unitario adicionada para cenarios de sucesso, CEP invalido e fallback apos erro de rede/CORS.
- Build/test local do frontend executado com sucesso para os arquivos alterados.

CODEBASE READING COMPLETE:
- `specs/grava-nois/DESIGN_SPEC.md`
- `grava_nois/README.md`
- `grava_nois/src/utils/viaCep.ts`
- `grava_nois/src/pages/ContactPage.vue`
- `grava_nois/src/pages/client/ClientProfile.vue`
- `grava_nois/src/pages/UserPage.vue` (mapeamento de comportamento similar por chamada direta de ViaCEP)
- `grava_nois/vite.config.ts`
- `grava_nois/package.json`
- `.codex/skills/pre-dev/SKILL.md`
- `.codex/references/agents/pre-dev.md`

REUSE ASSESSMENT:
- Decisao: adaptar `src/utils/viaCep.ts` para fallback de provedor.
- Reuso: manter todos callers existentes usando o mesmo utilitario.
- Justificativa: menor superficie de mudanca, reduz regressao e remove dependencia de um endpoint instavel para browser.

HARD RULES CHECK:
- HR-001 a HR-010: sem impacto direto (alteracao restrita a consulta publica de CEP no frontend, sem auth, upload, webhook, HMAC ou dados entre clientes).
- Status: aderente.

IMPACT MAP:
- Arquivo direto:
  - `grava_nois/src/utils/viaCep.ts`
- Arquivos chamadores impactados indiretamente:
  - `grava_nois/src/pages/ContactPage.vue`
  - `grava_nois/src/pages/client/ClientProfile.vue`
- Risco de cascata:
  - baixo; contrato de retorno permanece `Promise<ViaCepAddress | null>`.
- Limite de escopo seguro:
  - nao alterar fluxo de submit/formulario; ajustar apenas consulta e parse de CEP + testes.

TEST PLAN:
- Unitarios:
  - deve retornar `null` para CEP com tamanho invalido.
  - deve retornar endereco quando ViaCEP responder `200`.
  - deve tentar fallback quando ViaCEP falhar por erro de rede/CORS e retornar endereco pelo fallback.
  - deve retornar `null` quando provedores retornarem `erro/not found`.
- Integracao/manual:
  - validar autopreenchimento no `ContactPage.vue` com CEP valido.
  - validar autopreenchimento no `ClientProfile.vue` com CEP valido.
- Comandos:
  - `cd grava_nois && npm run test -- src/utils/viaCep.spec.ts`
  - `cd grava_nois && npm run build`

READY TO IMPLEMENT: YES

## [Execucao] Fallback de CEP no utilitario
- `src/utils/viaCep.ts`
  - adicionado fallback sequencial: `ViaCEP` -> `BrasilAPI`.
  - padronizado parse de payload para ambos provedores sem quebrar o contrato de retorno existente.
  - mantido timeout global com `AbortController`.
- `src/utils/viaCep.spec.ts`
  - cobertura para CEP invalido.
  - cobertura para sucesso direto no ViaCEP.
  - cobertura para erro de rede/CORS no ViaCEP com sucesso no fallback.
  - cobertura para cenario `not_found`.

## [Validacao] Testes e build
- Comando planejado no script do projeto:
  - `npm run test -- src/utils/viaCep.spec.ts`
  - resultado: falhou por flag legada do Vitest (`Unknown option --setupFiles`), problema preexistente no repo.
- Comando alternativo executado:
  - `npx vitest run src/utils/viaCep.spec.ts --environment jsdom --globals`
  - resultado: `1 passed`, `4 passed`.
- Build:
  - `npm run build`
  - resultado: sucesso.

## [Execucao] Padronizacao de chamada CEP na UserPage
- `src/pages/UserPage.vue`
  - removida chamada direta a `fetch("https://viacep.com.br/ws/...")`.
  - adotado `fetchViaCepAddress` para reaproveitar fallback de provedor e tratamento unificado.
  - normalizacao de CEP com `replace(/\D/g, "")` antes da consulta.
